# tabs

Base block: `tabs`. Generated for the WKND migration (Document Authoring project).

## Authoring
Author this block as a table with the block name `tabs` in the first cell.
Structural CSS only — brand styling is inherited from the global design system.

## Supported variations
None (single canonical structure).
